# Core dependencies (also listed in pyproject.toml)
torch>=2.4
torchvision>=0.19
lightning>=2.4
hydra-core>=1.3,<1.4
hydra-submitit-launcher>=1.2
omegaconf>=2.3
torchmetrics>=1.4
timm>=1.0
diffusers>=0.30
wandb>=0.17
submitit>=1.5

# stable-pretraining: pinned to the exact commit tested with this paper.
# This package provides the training loop, checkpointing, and SLURM integration.
stable-pretraining @ git+https://github.com/rbalestr-lab/stable-pretraining.git@99c0d978e2df86959d6918c6520396ecdb193da1
